version: "3.7"
services:
  devcontainer:
    image: devimages/eoan-xfce-novnc:1.0.3
    privileged: true
    ports:
      - "6080:6080"
    shm_size: 1gb
    volumes: 
      - devhome:/home/developer
volumes:
  devhome:
    driver: local