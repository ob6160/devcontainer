version: "3.7"
services:
  devcontainer:
    build: .
    image: zolika84/devcontainer:${VERSION}-eoan-xfce-novnc
    privileged: true
    ports:
      - "6080:6080"
    shm_size: 1gb
    command: bash -c "supervisord && sleep infinity"
    volumes: 
      - eoan-xfce-novnc:/home/developer:rw
volumes:
  eoan-xfce-novnc:
    external: true