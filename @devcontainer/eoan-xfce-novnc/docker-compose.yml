version: "3.7"
services:
  devcontainer:
    image: devimages/eoan-xfce-novnc
    privileged: true
    ports:
      - "6080"
    shm_size: 1gb
    volumes: 
      - devhome:/home/developer
volumes:
  devhome:
    driver: local