version: "3.7"
services:
  devcontainer-tagged:
    build: .
    image: devimages/${npm_package_tagname}:${npm_package_version}
  devcontainer-latest:
    build: .
    image: devimages/${npm_package_tagname}:latest
