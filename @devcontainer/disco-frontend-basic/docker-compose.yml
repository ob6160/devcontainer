version: "3.7"
services:
  devcontainer:
    image: devimages/disco-frontend-basic
    privileged: true
    ports:
      - "6080:6080"
    shm_size: 1gb
    command: bash -c "supervisord && sleep infinity"
    volumes: 
      - devhome:/home/developer
volumes:
  devhome:
    driver: local