version: "3.7"
services:
  devcontainer:
    image: devimages/disco-fullstack-xfce
    privileged: true
    ports:
      - "6080:6080"
    shm_size: 1gb
    volumes: 
      - devhome:/home/developer
volumes:
  devhome:
    driver: local